# 更新日志

## v1.4.1

新功能：
- 在 search_video API 中暴露一个 count 参数，用于控制每次搜索返回的结果数量，并允许通过 video_count_per_page 配置每页获取的视频数量。

增强改进：
- 使用适配并居中的缩略图在卡片尺寸内渲染视频封面，同时保持纵横比不变。

## v1.4.0

新功能：
- 引入可插拔的多平台视频搜索架构，并为各个平台实现各自的搜索器。
- 在现有 Bilibili 集成的基础上，新增抖音视频搜索、详情获取和下载支持。
- 暴露独立的 Bilibili 和抖音搜索命令，并允许 LLM 与命令流程针对特定平台进行调用。

增强改进：
- 使用通用的 `video_id`、`platform` 和 `cover` 字段统一各平台的视频元数据处理。
- 重构视频卡片渲染，使用共享封面图片缓存，并简化主题样式、文本和日志行为。
- 改进后台视频任务和 HTTP 会话的错误处理、日志消息以及资源清理。
- 调整配置以使用按平台划分的 cookies 和数据路径，使之兼容 AstrBot 插件数据目录。

文档：
- 更新 README 中的命令表，记录独立的 Bilibili 与抖音搜索命令。

## v1.3.0

新增功能：

- 暴露一个 LLM 工具入口，用于搜索视频，通过 LLM 选择最佳候选，并将其发送给用户。
- 增加一个由 LLM 驱动的流程，可以搜索候选视频，通过 bvid 选择合适的视频，并触发后台发送。

改进内容：

- 将基于命令的视频搜索处理重构到一个专门的流程类中，并由插件委托给该流程类处理。
- 将视频搜索、元数据获取、候选项格式化以及视频下载/发送逻辑抽取到可复用的 `VideoService` 中，并加入后台任务管理。
- 通过限定清晰度/编码格式并在合并前验证音视频流，使视频下载选择更加合理。
- 使用 `create_subprocess_exec` 使基于 ffmpeg 的音视频合并更加健壮，加入更完善的错误处理，并在 ffmpeg 不可用时提供回退方案。
- 增加用于获取单个视频元数据的 API 方法，以及一个用于格式化时长以供展示的辅助函数。
