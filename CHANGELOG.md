# 更新日志

## v1.3.0

新增功能：

- 暴露一个 LLM 工具入口，用于搜索视频，通过 LLM 选择最佳候选，并将其发送给用户。
- 增加一个由 LLM 驱动的流程，可以搜索候选视频，通过 bvid 选择合适的视频，并触发后台发送。

改进内容：

- 将基于命令的视频搜索处理重构到一个专门的流程类中，并由插件委托给该流程类处理。
- 将视频搜索、元数据获取、候选项格式化以及视频下载/发送逻辑抽取到可复用的 `VideoService` 中，并加入后台任务管理。
- 通过限定清晰度/编码格式并在合并前验证音视频流，使视频下载选择更加合理。
- 使用 `create_subprocess_exec` 使基于 ffmpeg 的音视频合并更加健壮，加入更完善的错误处理，并在 ffmpeg 不可用时提供回退方案。
- 增加用于获取单个视频元数据的 API 方法，以及一个用于格式化时长以供展示的辅助函数。
